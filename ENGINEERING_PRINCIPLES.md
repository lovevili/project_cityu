# 工程原理说明（数据集 A：NASA C-MAPSS FD001–FD004）

## 1. 工程问题定义
本项目面向航空发动机退化监测与维护决策，采用两类互补任务并行评估：
- **分类任务**：预测未来 30 周期内是否失效（风险告警）；
- **回归任务**：预测剩余使用寿命 RUL（维护窗口规划）。

## 2. 数据范围与一致性原则
- 数据来源：`dataset_A.zip` 中 C-MAPSS 全部子集 `FD001`–`FD004`。
- 统一处理流程：
  - 统一列定义（工况 + 传感器）；
  - 统一 RUL 标签构造（训练集基于单元最大循环；测试集基于官方 `RUL_FD00x.txt`）；
  - 统一风险标签定义（`RUL <= 30`）。
- 所有子集使用相同随机种子，保证实验可复现。

## 3. 特征工程原则
- 去除训练集中常量特征，保留有效工况与传感器变量。
- 构造时间依赖特征以刻画退化轨迹：
  - 滚动均值（趋势）；
  - 滚动标准差（波动）；
  - 一阶滞后（路径依赖）。
- 各子集采用一致的特征工程策略，实现可比性分析。

## 4. 建模与对比原则
- 分类至少两种算法：
  - 随机森林分类器（非线性与特征重要性）；
  - 逻辑回归（线性概率基线）。
- 回归至少两种算法：
  - 随机森林回归；
  - 梯度提升回归。
- 强制输出跨子集（FD001–FD004）对比结果，避免单子集结论偏差。

## 5. 评价指标体系
- 分类核心指标：**Recall**、**AP/AUC-PR**、**ROC-AUC**。
- 分类补充指标：Precision、F1、Brier（概率校准质量）。
- 回归核心指标：**RMSE**。
- 回归补充指标：MAE、R²。

## 6. 可视化与可解释性标准
Notebook 至少包含以下专业图表：
1. 关键传感器退化趋势图（按标准化生命周期）；
2. 各子集最优分类器混淆矩阵；
3. 各子集最优分类器 PR 曲线；
4. 各子集最优分类器校准曲线；
5. 跨子集特征重要性汇总图；
6. 各子集最优回归器误差分布图。

## 7. 中文字体与展示质量
- matplotlib 需配置 CJK 字体优先级（如 Noto Sans CJK、SimHei 等）并提供 fallback。
- 目标是避免中文标题/标签缺字（missing glyph）告警，保证报告可读性。

## 8. 交付标准
- Notebook 应可端到端运行，形成“数据概览 → 建模对比 → 可视化诊断 → 工程结论”的完整叙事链。
- 结论必须明确指出各子集、各方法优劣及工程落地建议。
