# 工程原理说明（数据集 A：NASA C-MAPSS）

## 1. 工程问题定义
本项目针对航空涡扇发动机退化风险建模，目标包括：
- **剩余使用寿命（RUL）回归预测**，用于维护窗口规划；
- **n 周期内失效风险分类**（本项目取 n=30），用于提前告警与风险分级。

## 2. 风险建模思想
- 退化系统存在明显的**时间依赖性**，因此在原始传感器与工况变量基础上构造：
  - 滚动均值/滚动标准差（捕捉漂移与波动）；
  - 滞后特征（反映路径依赖）。
- 对分类任务输出**概率**而非仅硬标签，便于风险阈值决策。

## 3. 数据与特征处理原则
- 数据来源：`dataset_A.zip` 中 C-MAPSS FD001 子集。
- 标签构造：
  - 训练集 RUL 由“单元最大循环数 - 当前循环数”得到；
  - 测试集使用官方 `RUL_FD001.txt` 还原每个循环的真实 RUL。
- 特征筛选：移除常量特征，保留有效工况与传感器变量。

## 4. 模型与不确定性
- 分类模型至少比较两类算法：
  - 随机森林分类器（非线性、可解释特征重要性）；
  - 逻辑回归（线性基线，概率可解释）。
- 回归模型至少比较两类算法：
  - 随机森林回归；
  - 梯度提升回归。
- 不确定性量化：
  - 分类：使用失效概率分布；
  - 回归：使用随机森林树集的分位数近似预测区间。

## 5. 风险导向评价指标
- **召回率（Sensitivity/Recall）**：关键失效检出能力；
- **AUC-PR / AP**：不平衡风险场景下更关注正类表现；
- **RMSE**：RUL 预测误差规模。

## 6. 工程可视化设计
Notebook 提供三类核心可视化：
1. **传感器趋势图**：展示退化过程中的关键传感器趋势；
2. **混淆矩阵**：展示失效风险识别效果；
3. **特征重要性图**：识别主要风险驱动变量。

## 7. 决策层解释
- 当模型给出高失效概率且预测区间下界较低时，应优先安排预防性维护；
- 对于误报可通过阈值与维护成本联合优化；
- 后续可扩展到 FD002–FD004 或跨工况迁移评估以提升泛化能力。
